{
  "name": "css-compiler",
  "version": "1.0.0",
  "description": "css to old css",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "postcss in/style.css -o out/style.css",
    "watch:css": "postcss src/style.css -o dist/style.css --watch"
  },
  "author": "Klym T.",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "postcss-cli": "^11.0.1",
    "postcss-nested": "^7.0.2"
  }
}
